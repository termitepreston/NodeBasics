const { PI } = Math;

const area = (r) => PI * r * r;
const circumference = (r) => 2 * PI * r;
const add = (a, b) => a + b;
const subtract = (a, b) => a - b;

export { area, circumference, add, subtract };
